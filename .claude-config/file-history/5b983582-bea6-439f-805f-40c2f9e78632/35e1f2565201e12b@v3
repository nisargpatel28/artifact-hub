{
  "mcpServers": {
    "artifact-hub": {
      "_comment": "Option A (recommended after building locally): point node at the compiled output.",
      "command": "node",
      "args": ["/absolute/path/to/artifact-hub/apps/mcp/dist/index.js"],
      "env": {
        "API_BASE_URL": "https://your-api.railway.app",
        "API_KEY": "your-api-key"
      }
    }
  }
}
